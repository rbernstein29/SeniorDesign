<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings — VulnScan</title>
    <%= csrf_meta_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Sora:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg:        #080c14;
            --surface:   #0d1525;
            --surface2:  #111b30;
            --border:    rgba(59,130,246,0.15);
            --blue:      #3b82f6;
            --blue-dim:  rgba(59,130,246,0.12);
            --blue-glow: rgba(59,130,246,0.35);
            --green:     #10b981;
            --orange:    #f59e0b;
            --red:       #ef4444;
            --text:      #e2e8f0;
            --muted:     #64748b;
            --mono:      'JetBrains Mono', monospace;
            --sans:      'Sora', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: var(--sans);
            background: var(--bg);
            color: var(--text);
            min-height: 100vh;
        }

        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background-image:
                linear-gradient(rgba(59,130,246,0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(59,130,246,0.03) 1px, transparent 1px);
            background-size: 40px 40px;
            pointer-events: none;
            z-index: 0;
        }

        .layout {
            position: relative;
            z-index: 1;
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px 30px 40px 84px;
        }

        .page-header {
            background: var(--surface);
            border: 1px solid var(--border);
            padding: 28px 36px;
            border-radius: 16px;
            margin-bottom: 28px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .page-title { font-size: 26px; font-weight: 700; color: #f1f5f9; }
        .page-sub { color: var(--muted); margin-top: 4px; font-size: 13px; font-family: var(--mono); }

        .settings-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }

        @media (max-width: 900px) { .settings-grid { grid-template-columns: 1fr; } }

        .settings-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 16px;
            overflow: hidden;
            margin-bottom: 0;
        }

        .settings-card.full-width {
            grid-column: 1 / -1;
        }

        .card-header {
            padding: 20px 28px;
            border-bottom: 1px solid rgba(255,255,255,0.06);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .card-header-icon {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            background: var(--blue-dim);
            border: 1px solid rgba(59,130,246,0.2);
            flex-shrink: 0;
        }

        .card-title { font-size: 15px; font-weight: 600; color: #f1f5f9; }
        .card-subtitle { font-family: var(--mono); font-size: 10px; color: var(--muted); letter-spacing: 1px; margin-top: 2px; }

        .settings-body { padding: 24px 28px; display: flex; flex-direction: column; gap: 20px; }

        .field-group { display: flex; flex-direction: column; gap: 6px; }

        .field-label {
            font-family: var(--mono);
            font-size: 11px;
            font-weight: 600;
            color: var(--muted);
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .field-input {
            background: var(--surface2);
            border: 1px solid rgba(255,255,255,0.08);
            border-radius: 8px;
            padding: 11px 16px;
            color: #cbd5e1;
            font-size: 13px;
            font-family: var(--sans);
            width: 100%;
            transition: border-color 0.2s;
            outline: none;
        }

        .field-input:focus { border-color: var(--blue); }
        .field-input:disabled { opacity: 0.5; cursor: not-allowed; }
        .field-input.mono { font-family: var(--mono); font-size: 12px; }

        .field-hint { font-size: 12px; color: var(--muted); }

        .toggle-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 14px 0;
            border-bottom: 1px solid rgba(255,255,255,0.04);
        }

        .toggle-row:last-child { border-bottom: none; padding-bottom: 0; }
        .toggle-row:first-child { padding-top: 0; }

        .toggle-label { font-size: 13px; color: #cbd5e1; font-weight: 500; }
        .toggle-hint { font-size: 12px; color: var(--muted); margin-top: 2px; }

        .toggle {
            position: relative;
            width: 44px;
            height: 24px;
            flex-shrink: 0;
        }

        .toggle input { opacity: 0; width: 0; height: 0; }

        .toggle-slider {
            position: absolute;
            inset: 0;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.12);
            border-radius: 24px;
            cursor: pointer;
            transition: 0.3s;
        }

        .toggle-slider::before {
            content: '';
            position: absolute;
            width: 16px;
            height: 16px;
            left: 3px;
            top: 50%;
            transform: translateY(-50%);
            background: var(--muted);
            border-radius: 50%;
            transition: 0.3s;
        }

        .toggle input:checked + .toggle-slider { background: rgba(16,185,129,0.2); border-color: rgba(16,185,129,0.4); }
        .toggle input:checked + .toggle-slider::before { transform: translate(20px, -50%); background: var(--green); box-shadow: 0 0 8px var(--green); }

        .btn {
            padding: 11px 22px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 13px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.2s;
            border: none;
            font-family: var(--sans);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--blue), #1d4ed8);
            color: white;
            box-shadow: 0 4px 16px var(--blue-glow);
        }

        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px var(--blue-glow); }

        .btn-secondary {
            background: transparent;
            border: 1px solid rgba(255,255,255,0.1);
            color: #94a3b8;
        }

        .btn-secondary:hover { border-color: var(--blue); color: #60a5fa; background: var(--blue-dim); }

        .btn-danger {
            background: rgba(239,68,68,0.12);
            border: 1px solid rgba(239,68,68,0.25);
            color: #f87171;
        }

        .btn-danger:hover { background: rgba(239,68,68,0.2); }

        .btn-sm { padding: 7px 16px; font-size: 12px; font-family: var(--mono); }

        .card-footer {
            padding: 16px 28px;
            border-top: 1px solid rgba(255,255,255,0.06);
            display: flex;
            justify-content: flex-end;
            gap: 10px;
        }

        .status-row {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 12px 16px;
            background: var(--surface2);
            border: 1px solid rgba(255,255,255,0.06);
            border-radius: 8px;
        }

        .status-dot-sm { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
        .dot-green-sm { background: var(--green); box-shadow: 0 0 6px var(--green); animation: pulse 2s infinite; }
        .dot-red-sm   { background: var(--red);   box-shadow: 0 0 6px var(--red); }

        @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }

        .status-text { font-family: var(--mono); font-size: 12px; color: #94a3b8; }

        .danger-zone {
            border-color: rgba(239,68,68,0.2);
        }

        .danger-zone .card-header { border-bottom-color: rgba(239,68,68,0.1); }

        .danger-zone .card-header-icon {
            background: rgba(239,68,68,0.1);
            border-color: rgba(239,68,68,0.2);
        }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        .page-header    { animation: fadeUp 0.4s ease both; }
        .settings-grid  { animation: fadeUp 0.4s 0.08s ease both; }
    </style>
</head>
<body>

    <%= render 'shared/sidebar', current_page: 'settings' %>

    <div class="layout">

        <div class="page-header">
            <div>
                <div class="page-title">Settings</div>
                <div class="page-sub">// platform configuration and preferences</div>
            </div>
        </div>

        <div class="settings-grid">

            <!-- Server Configuration -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-header-icon">🖥</div>
                    <div>
                        <div class="card-title">Server Configuration</div>
                        <div class="card-subtitle">INFRASTRUCTURE</div>
                    </div>
                </div>
                <div class="settings-body">
                    <div class="status-row">
                        <div class="status-dot-sm dot-green-sm"></div>
                        <span class="status-text">Database connected — PostgreSQL @ 100.69.88.107</span>
                    </div>
                    <div class="field-group">
                        <div class="field-label">Scanner Server IP</div>
                        <input class="field-input mono" type="text" value="100.69.88.107" disabled>
                        <div class="field-hint">Tailscale IP of the Linux scanning server</div>
                    </div>
                    <div class="field-group">
                        <div class="field-label">Rails Port</div>
                        <input class="field-input mono" type="text" value="3000" disabled>
                    </div>
                    <div class="field-group">
                        <div class="field-label">Agent Tunnel Port Range</div>
                        <input class="field-input mono" type="text" value="9000 – 9999" disabled>
                        <div class="field-hint">Ports allocated to SSH reverse tunnels from agents</div>
                    </div>
                </div>
            </div>

            <!-- Account -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-header-icon">👤</div>
                    <div>
                        <div class="card-title">Account</div>
                        <div class="card-subtitle">USER PROFILE</div>
                    </div>
                </div>
                <div class="settings-body">
                    <div class="field-group">
                        <div class="field-label">Email</div>
                        <input class="field-input" type="email" value="<%= Current.user&.email_address || '' %>" disabled>
                    </div>
                    <div class="field-group">
                        <div class="field-label">New Password</div>
                        <input class="field-input" type="password" placeholder="••••••••" disabled>
                        <div class="field-hint">Password changes coming soon</div>
                    </div>
                </div>
                <div class="card-footer">
                    <%= link_to "Sign Out", destroy_session_path, data: { turbo_method: :delete }, class: "btn btn-danger btn-sm" %>
                </div>
            </div>

            <!-- Scanner Defaults -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-header-icon">🔬</div>
                    <div>
                        <div class="card-title">Scanner Defaults</div>
                        <div class="card-subtitle">SCAN BEHAVIOR</div>
                    </div>
                </div>
                <div class="settings-body">
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Auto-generate reports after scan</div>
                            <div class="toggle-hint">Create a report automatically when a scan completes</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" checked disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Route scans through agent tunnel</div>
                            <div class="toggle-hint">Use SOCKS5 proxy from connected agents</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" checked disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Enable aggressive scan mode</div>
                            <div class="toggle-hint">Faster but more detectable — use with caution</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="field-hint" style="margin-top: 4px;">Editable settings coming in a future release</div>
                </div>
            </div>

            <!-- Notifications -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-header-icon">🔔</div>
                    <div>
                        <div class="card-title">Notifications</div>
                        <div class="card-subtitle">ALERTS</div>
                    </div>
                </div>
                <div class="settings-body">
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Agent goes offline</div>
                            <div class="toggle-hint">Alert when a connected agent stops heartbeating</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Critical vulnerability found</div>
                            <div class="toggle-hint">Alert on CVSS 9.0+ findings</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="toggle-row">
                        <div>
                            <div class="toggle-label">Scan completed</div>
                            <div class="toggle-hint">Alert when any scan finishes</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" disabled>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                    <div class="field-hint" style="margin-top: 4px;">Notification delivery coming in a future release</div>
                </div>
            </div>

            <!-- Danger Zone -->
            <div class="settings-card danger-zone full-width">
                <div class="card-header">
                    <div class="card-header-icon">⚠</div>
                    <div>
                        <div class="card-title">Danger Zone</div>
                        <div class="card-subtitle">DESTRUCTIVE ACTIONS</div>
                    </div>
                </div>
                <div class="settings-body" style="flex-direction: row; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 16px;">
                    <div>
                        <div style="font-size: 13px; font-weight: 600; color: #f1f5f9; margin-bottom: 4px;">Delete All Agents</div>
                        <div style="font-size: 12px; color: var(--muted);">Permanently remove all deployed agents and their SSH keys</div>
                    </div>
                    <%= button_to "Delete All Agents", agents_path,
                        method: :delete,
                        data: { confirm: "This will delete ALL agents. This cannot be undone." },
                        class: "btn btn-danger btn-sm",
                        disabled: true,
                        style: "opacity: 0.5; cursor: not-allowed;" %>
                </div>
            </div>

        </div>
    </div>

</body>
</html>
