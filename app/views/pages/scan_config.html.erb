<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scan Configuration</title>
    <style>
        :root {
            --primary-blue: #3b82f6;
            --dark-blue: #1d4ed8;
            --bg-color: #f3f4f6;
            --border-color: #dee2e6;
            --text-color: #343a40;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            padding: 40px;
            margin: 0;
        }

        .card {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            width: 100%;
            max-width: 900px;
        }

        h1 {
            text-align: center;
            color: #1f2937;
            font-size: 24px;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f3f4f6;
        }

        /* Grid Layout */
        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            column-gap: 40px;
            row-gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            margin-bottom: 10px;
        }

        label {
            font-weight: 600;
            font-size: 14px;
            color: #4b5563;
            margin-bottom: 8px;
        }

        label.required::after {
            content: " *";
            color: #ef4444;
        }

        input[type="text"],
        input[type="datetime-local"],
        select {
            padding: 12px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 14px;
            color: #333;
            transition: all 0.2s ease;
            background-color: #f9fafb;
        }

        input:focus, select:focus {
            outline: none;
            border-color: var(--primary-blue);
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }

        .boxed-group {
            background-color: #f9fafb;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            cursor: pointer;
            font-weight: 500;
        }

        input[type="radio"], input[type="checkbox"] {
            accent-color: var(--primary-blue);
            width: 16px;
            height: 16px;
            cursor: pointer;
        }

        /* Buttons Container */
        .btn-group {
            margin-top: 40px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        /* Import Button (Subtle) */
        .btn-outline {
            background: white;
            border: 2px dashed #cbd5e1;
            color: #64748b;
            padding: 15px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 14px;
            width: 100%;
            text-align: center;
        }

        .btn-outline:hover {
            border-color: var(--primary-blue);
            color: var(--primary-blue);
            background-color: #eff6ff;
        }

        /* --- THE SATISFYING BUTTON --- */
        .btn-3d {
            position: relative;
            width: 100%;
            display: inline-block;
            cursor: pointer;
            outline: none;
            border: 0;
            vertical-align: middle;
            text-decoration: none;
            background: transparent;
            padding: 0;
            font-size: 16px;
            font-family: inherit;
        }

        .btn-3d-content {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 60px; /* Nice and chunky */
            padding: 0 20px;
            background: linear-gradient(to bottom, #3b82f6, #2563eb); /* Subtle gradient */
            border-radius: 12px;
            color: white;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: transform 0.1s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 2;
            
            /* The 3D Depth (Sides of the button) */
            border-bottom: 6px solid #1e40af; 
            box-shadow: 0 10px 20px -8px rgba(37, 99, 235, 0.5);
        }

        /* Hover State: Slight brightness */
        .btn-3d:hover .btn-3d-content {
            background: linear-gradient(to bottom, #4f8df9, #326bf5);
            transform: translateY(-2px);
        }

        /* Active/Pressed State: The Clunk */
        .btn-3d:active .btn-3d-content {
            transform: translateY(4px); /* Moves down 4px */
            border-bottom-width: 2px; /* Border shrinks */
            box-shadow: none; /* Shadow disappears */
        }

        /* Loading State Animation */
        .btn-3d.loading .btn-3d-content {
            background: #4b5563;
            border-bottom-color: #1f2937;
            pointer-events: none;
            transform: translateY(4px);
            border-bottom-width: 2px;
        }

        /* Spinner */
        .spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
            margin-right: 10px;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

    </style>
</head>
<body>

    <div class="card">
        <h1>Scan Configuration</h1>
        
        <form id="scanForm">
            <div class="form-grid">
                
                <div class="column">
                    <div class="form-group">
                        <label class="required">Scan Mode</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <input type="radio" name="scanMode" value="safe" required> Safe
                            </label>
                            <label class="radio-option">
                                <input type="radio" name="scanMode" value="exploits" required> Exploits
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="required">Target Scope</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <input type="radio" name="scope" value="individual" checked required> Individual Network
                            </label>
                            <label class="radio-option">
                                <input type="radio" name="scope" value="range" required> Range of Networks
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="network" class="required">Network / Network Range</label>
                        <input type="text" id="network" placeholder="e.g. 192.168.1.1/24" required>
                    </div>

                    <div class="form-group">
                        <label for="exclude" class="required">Exclude Network</label>
                        <input type="text" id="exclude" placeholder="e.g. 192.168.1.5" required>
                    </div>

                    <div class="form-group">
                        <label for="port" class="required">Port / Port Range</label>
                        <input type="text" id="port" placeholder="e.g. 80, 443, 8000-8080" required>
                    </div>

                    <div class="form-group">
                        <label for="os" class="required">Operating System</label>
                        <select id="os" required>
                            <option value="">Select OS</option>
                            <option value="win11">Windows 10/11</option>
                            <option value="server2019">Windows Server 2019</option>
                            <option value="linux">Linux (Ubuntu/CentOS)</option>
                            <option value="macos">macOS</option>
                        </select>
                    </div>
                </div>

                <div class="column">
                    <div class="form-group">
                        <label for="asset" class="required">Asset Types</label>
                        <select id="asset" required>
                            <option value="server">Server</option>
                            <option value="workstation">Workstation</option>
                            <option value="network_device">Network Device</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="scanType" class="required">Scan Type</label>
                        <select id="scanType" required>
                            <option value="full">Full</option>
                            <option value="quick">Quick</option>
                            <option value="custom">Custom</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="credential" class="required">Credential</label>
                        <input type="text" id="credential" placeholder="Enter credential ID or Path" required>
                    </div>

                    <div class="form-group">
                        <label for="schedule">Schedule</label>
                        <input type="datetime-local" id="schedule">
                    </div>

                    <div class="form-group">
                        <label for="cve">CVE ID</label>
                        <input type="text" id="cve" placeholder="e.g. CVE-2024-XXXX">
                    </div>

                    <div class="form-group">
                        <label class="required">Report Format</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <input type="checkbox" name="format" value="json" checked required> JSON
                            </label>
                            <label class="radio-option">
                                <input type="checkbox" name="format" value="pdf"> PDF
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="btn-group">
                <input type="file" id="jsonFile" accept=".json" style="display: none;">
                <button type="button" class="btn-outline" onclick="document.getElementById('jsonFile').click()">
                    📂 Import Asset JSON <span id="file-upload-name"></span>
                </button>

                <button type="submit" class="btn-3d" id="generateBtn">
                    <span class="btn-3d-content">
                        <span class="spinner"></span>
                        <span class="btn-text">Add Asset</span>
                    </span>
                </button>
            </div>
        </form>
    </div>

    <script>
        // File Upload Logic
        document.getElementById('jsonFile').addEventListener('change', function(e) {
            const fileName = e.target.files[0] ? e.target.files[0].name : "";
            const label = document.getElementById('file-upload-name');
            label.textContent = fileName ? `— ${fileName}` : "";
            label.style.fontWeight = "bold";
            label.style.color = "#2563eb";
        });

        // Form Submit & Button Animation Logic
        document.getElementById('scanForm').addEventListener('submit', function(e) {
            e.preventDefault(); // Stop actual submit for demo
            
            const btn = document.getElementById('generateBtn');
            const spinner = btn.querySelector('.spinner');
            const text = btn.querySelector('.btn-text');

            // 1. Add loading class to button (changes color/state)
            btn.classList.add('loading');
            spinner.style.display = 'inline-block';
            text.textContent = 'Scanning...';

            // 2. Simulate a delay (e.g., 2 seconds)
            setTimeout(() => {
                // 3. Success State
                spinner.style.display = 'none';
                text.textContent = '✓ Asset Ready';
                btn.querySelector('.btn-3d-content').style.background = '#10b981'; // Green
                btn.querySelector('.btn-3d-content').style.borderBottomColor = '#059669'; // Dark Green border
                
                // 4. Reset after another second (optional)
                setTimeout(() => {
                    btn.classList.remove('loading');
                    text.textContent = 'Add Asset';
                    btn.querySelector('.btn-3d-content').style.background = ''; // Reset to CSS default
                    btn.querySelector('.btn-3d-content').style.borderBottomColor = ''; 
                }, 3000);
                
            }, 2000);
        });
    </script>
</body>
</html>
