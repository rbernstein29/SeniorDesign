<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <% content_for :title, "Aegis - Dashboard" %>
  <%= csrf_meta_tags %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Sora:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg:       #080c14;
      --surface:  #0d1525;
      --surface2: #111b30;
      --border:   rgba(59,130,246,0.15);
      --blue:     #3b82f6;
      --blue-dim: rgba(59,130,246,0.12);
      --blue-glow:rgba(59,130,246,0.35);
      --cyan:     #22d3ee;
      --green:    #10b981;
      --orange:   #f59e0b;
      --red:      #ef4444;
      --text:     #e2e8f0;
      --muted:    #64748b;
      --mono:     'JetBrains Mono', monospace;
      --sans:     'Sora', sans-serif;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: var(--sans);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── Background Grid ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(59,130,246,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(59,130,246,0.04) 1px, transparent 1px);
      background-size: 40px 40px;
      pointer-events: none;
      z-index: 0;
    }

    /* ── Glow blobs ── */
    .bg-glow {
      position: fixed;
      border-radius: 50%;
      filter: blur(120px);
      pointer-events: none;
      z-index: 0;
    }
    .glow-1 { width: 500px; height: 500px; background: rgba(59,130,246,0.08); top: -100px; right: -100px; }
    .glow-2 { width: 400px; height: 400px; background: rgba(34,211,238,0.05); bottom: 100px; left: -100px; }

    /* ── Layout ── */
    .layout {
      position: relative;
      z-index: 1;
      max-width: 1300px;
      margin: 0 auto;
      padding: 40px 30px 40px 84px; /* left padding for toggle btn */
    }

    /* ── Top Bar ── */
    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 48px;
    }

    .topbar-brand {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .brand-logo {
      width: 44px;
      height: 44px;
      background: linear-gradient(135deg, var(--blue), #1d4ed8);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      box-shadow: 0 0 24px var(--blue-glow);
    }

    .brand-name {
      font-family: var(--mono);
      font-size: 20px;
      font-weight: 700;
      color: #f1f5f9;
      letter-spacing: 1px;
    }

    .brand-version {
      font-family: var(--mono);
      font-size: 10px;
      color: var(--muted);
      letter-spacing: 2px;
    }

    .topbar-meta {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .live-badge {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 6px 14px;
      background: rgba(16,185,129,0.1);
      border: 1px solid rgba(16,185,129,0.25);
      border-radius: 20px;
      font-family: var(--mono);
      font-size: 11px;
      font-weight: 600;
      color: var(--green);
      letter-spacing: 1px;
    }

    .live-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--green);
      box-shadow: 0 0 8px var(--green);
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50%       { opacity: 0.6; transform: scale(0.85); }
    }

    .topbar-time {
      font-family: var(--mono);
      font-size: 12px;
      color: var(--muted);
    }

    /* ── Hero Section ── */
    .hero {
      margin-bottom: 48px;
    }

    .hero-greeting {
      font-family: var(--mono);
      font-size: 12px;
      color: var(--blue);
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 12px;
    }

    .hero-title {
      font-size: 42px;
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 14px;
      background: linear-gradient(135deg, #f1f5f9 30%, var(--blue) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-sub {
      font-size: 15px;
      color: var(--muted);
      max-width: 480px;
      line-height: 1.6;
    }

    /* ── Stats Row ── */
    .stats-row {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      margin-bottom: 40px;
    }

    @media (max-width: 900px) {
      .stats-row { grid-template-columns: repeat(2, 1fr); }
    }

    .stat-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 22px 24px;
      position: relative;
      overflow: hidden;
      transition: border-color 0.3s, box-shadow 0.3s;
    }

    .stat-card:hover {
      border-color: rgba(59,130,246,0.35);
      box-shadow: 0 0 24px rgba(59,130,246,0.08);
    }

    .stat-card::after {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: var(--accent, var(--blue));
    }

    .stat-card.green { --accent: var(--green); }
    .stat-card.orange { --accent: var(--orange); }
    .stat-card.red { --accent: var(--red); }
    .stat-card.cyan { --accent: var(--cyan); }

    .stat-label {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 10px;
    }

    .stat-value {
      font-family: var(--mono);
      font-size: 34px;
      font-weight: 700;
      color: #f1f5f9;
      line-height: 1;
      margin-bottom: 6px;
    }

    .stat-delta {
      font-size: 12px;
      color: var(--muted);
    }

    .stat-delta.up { color: var(--green); }
    .stat-delta.warn { color: var(--orange); }
    .stat-delta.bad { color: var(--red); }

    /* ── Navigation Cards ── */
    .section-header {
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .section-header::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    .nav-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      margin-bottom: 40px;
    }

    @media (max-width: 900px) {
      .nav-grid { grid-template-columns: repeat(2, 1fr); }
    }

    @media (max-width: 580px) {
      .nav-grid { grid-template-columns: 1fr; }
    }

    .nav-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 28px;
      text-decoration: none;
      display: flex;
      flex-direction: column;
      gap: 14px;
      position: relative;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4,0,0.2,1);
      cursor: pointer;
      group: true;
    }

    .nav-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at var(--mx, 50%) var(--my, 50%), var(--card-glow, rgba(59,130,246,0.08)) 0%, transparent 60%);
      opacity: 0;
      transition: opacity 0.4s;
    }

    .nav-card:hover {
      border-color: rgba(59,130,246,0.4);
      transform: translateY(-3px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.4), 0 0 30px rgba(59,130,246,0.08);
    }

    .nav-card:hover::before { opacity: 1; }

    .card-icon-wrap {
      width: 52px;
      height: 52px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      background: var(--icon-bg, rgba(59,130,246,0.12));
      border: 1px solid var(--icon-border, rgba(59,130,246,0.2));
      transition: box-shadow 0.3s;
      flex-shrink: 0;
    }

    .nav-card:hover .card-icon-wrap {
      box-shadow: 0 0 20px var(--icon-glow, var(--blue-glow));
    }

    .card-title {
      font-size: 16px;
      font-weight: 700;
      color: #f1f5f9;
      margin-bottom: 4px;
    }

    .card-desc {
      font-size: 13px;
      color: var(--muted);
      line-height: 1.5;
    }

    .card-arrow {
      position: absolute;
      top: 24px;
      right: 24px;
      width: 28px;
      height: 28px;
      border-radius: 50%;
      border: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--muted);
      font-size: 13px;
      transition: all 0.3s;
    }

    .nav-card:hover .card-arrow {
      background: var(--blue);
      border-color: var(--blue);
      color: white;
      transform: translateX(2px);
    }

    .card-tag {
      display: inline-block;
      padding: 3px 10px;
      border-radius: 8px;
      font-family: var(--mono);
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 1px;
      background: var(--blue-dim);
      color: var(--blue);
      border: 1px solid rgba(59,130,246,0.2);
      width: fit-content;
    }

    /* Card color variants */
    .card-scan {
      --icon-bg: rgba(34,211,238,0.1);
      --icon-border: rgba(34,211,238,0.2);
      --icon-glow: rgba(34,211,238,0.3);
      --card-glow: rgba(34,211,238,0.08);
    }

    .card-assets {
      --icon-bg: rgba(59,130,246,0.12);
      --icon-border: rgba(59,130,246,0.2);
      --icon-glow: rgba(59,130,246,0.35);
      --card-glow: rgba(59,130,246,0.08);
    }

    .card-agents {
      --icon-bg: rgba(139,92,246,0.12);
      --icon-border: rgba(139,92,246,0.2);
      --icon-glow: rgba(139,92,246,0.35);
      --card-glow: rgba(139,92,246,0.08);
    }

    .card-reports {
      --icon-bg: rgba(16,185,129,0.12);
      --icon-border: rgba(16,185,129,0.2);
      --icon-glow: rgba(16,185,129,0.35);
      --card-glow: rgba(16,185,129,0.08);
    }

    .card-settings {
      --icon-bg: rgba(245,158,11,0.12);
      --icon-border: rgba(245,158,11,0.2);
      --icon-glow: rgba(245,158,11,0.35);
      --card-glow: rgba(245,158,11,0.08);
    }

    /* ── Activity Feed ── */
    .bottom-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }

    @media (max-width: 800px) {
      .bottom-row { grid-template-columns: 1fr; }
    }

    .panel {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 16px;
      overflow: hidden;
    }

    .panel-header {
      padding: 18px 24px;
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .panel-title {
      font-size: 13px;
      font-weight: 600;
      color: #f1f5f9;
    }

    .panel-sub {
      font-family: var(--mono);
      font-size: 10px;
      color: var(--muted);
      letter-spacing: 1px;
    }

    .activity-item {
      display: flex;
      align-items: flex-start;
      gap: 14px;
      padding: 16px 24px;
      border-bottom: 1px solid rgba(255,255,255,0.04);
      transition: background 0.2s;
    }

    .activity-item:last-child { border-bottom: none; }
    .activity-item:hover { background: rgba(255,255,255,0.02); }

    .activity-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      margin-top: 5px;
      flex-shrink: 0;
    }

    .dot-green { background: var(--green); box-shadow: 0 0 6px var(--green); }
    .dot-orange { background: var(--orange); box-shadow: 0 0 6px var(--orange); }
    .dot-red { background: var(--red); box-shadow: 0 0 6px var(--red); }
    .dot-blue { background: var(--blue); box-shadow: 0 0 6px var(--blue); }

    .activity-text {
      font-size: 13px;
      color: #cbd5e1;
      line-height: 1.5;
      flex: 1;
    }

    .activity-text strong { color: #f1f5f9; }

    .activity-time {
      font-family: var(--mono);
      font-size: 10px;
      color: var(--muted);
      white-space: nowrap;
      margin-top: 3px;
    }

    /* Quick actions */
    .quick-btn {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 14px 20px;
      margin: 6px 12px;
      border-radius: 10px;
      background: var(--surface2);
      border: 1px solid var(--border);
      color: var(--text);
      text-decoration: none;
      font-size: 13px;
      font-weight: 500;
      transition: all 0.2s;
    }

    .quick-btn:hover {
      background: rgba(59,130,246,0.1);
      border-color: rgba(59,130,246,0.3);
      color: #60a5fa;
    }

    .quick-btn-icon {
      width: 32px;
      height: 32px;
      border-radius: 8px;
      background: var(--blue-dim);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      flex-shrink: 0;
    }

    .quick-btn-label { flex: 1; }
    .quick-btn-arrow { color: var(--muted); font-size: 12px; }

    /* Animations */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .hero         { animation: fadeUp 0.5s ease both; }
    .stats-row    { animation: fadeUp 0.5s 0.1s ease both; }
    .nav-grid     { animation: fadeUp 0.5s 0.2s ease both; }
    .bottom-row   { animation: fadeUp 0.5s 0.3s ease both; }
  </style>
</head>
<body>
  <div class="bg-glow glow-1"></div>
  <div class="bg-glow glow-2"></div>

  <!-- Sidebar Navigation -->
  <%= render 'shared/sidebar', current_page: 'home' %>

  <div class="layout">
    <!-- Top Bar -->
    <div class="topbar">
      <div class="topbar-brand">
        <div class="brand-logo">🔍</div>
        <div>
          <div class="brand-name">Aegis</div>
          <div class="brand-version">SECURITY PLATFORM v1.0</div>
        </div>
      </div>
      <div class="topbar-meta">
        <div class="live-badge">
          <div class="live-dot"></div>
          SYSTEMS ONLINE
        </div>
        <div class="topbar-time" id="clock"></div>
      </div>
    </div>

    <!-- Hero -->
    <div class="hero">
      <div class="hero-greeting">// Welcome back, <%= Current.session&.user&.name&.split(' ')&.first&.capitalize || 'Analyst' %></div>
      <h1 class="hero-title">Security<br>Operations Center</h1>
      <p class="hero-sub">Monitor vulnerabilities, manage assets, and coordinate scanning agents from a single command center.</p>
    </div>

    <!-- Stats -->
    <div class="stats-row">
      <div class="stat-card green">
        <div class="stat-label">Total Assets</div>
        <div class="stat-value"><%= @assets&.count || 0 %></div>
        <div class="stat-delta up">▲ Configured targets</div>
      </div>
      <div class="stat-card cyan">
        <div class="stat-label">Active Scans</div>
        <div class="stat-value">0</div>
        <div class="stat-delta">No scans running</div>
      </div>
      <div class="stat-card orange">
        <div class="stat-label">Open Findings</div>
        <div class="stat-value">—</div>
        <div class="stat-delta warn">Pending first scan</div>
      </div>
      <div class="stat-card red">
        <div class="stat-label">Critical Vulns</div>
        <div class="stat-value">—</div>
        <div class="stat-delta bad">Run a scan to detect</div>
      </div>
    </div>

    <!-- Navigation Cards -->
    <div class="section-header">Navigate</div>
    <div class="nav-grid">

      <a href="<%= scans_path rescue '#' %>" class="nav-card card-scan">
        <div class="card-arrow">→</div>
        <div class="card-icon-wrap">🔬</div>
        <div>
          <div class="card-title">Run a Scan</div>
          <div class="card-desc">Configure and launch vulnerability scans against your targets.</div>
        </div>
        <div class="card-tag">SCANNER</div>
      </a>

      <a href="<%= assets_path %>" class="nav-card card-assets">
        <div class="card-arrow">→</div>
        <div class="card-icon-wrap">🖥️</div>
        <div>
          <div class="card-title">Asset Management</div>
          <div class="card-desc">Add, edit, and organize your network targets and scan configurations.</div>
        </div>
        <div class="card-tag">ASSETS</div>
      </a>

      <a href="<%= agents_path rescue '#' %>" class="nav-card card-agents">
        <div class="card-arrow">→</div>
        <div class="card-icon-wrap">🤖</div>
        <div>
          <div class="card-title">Agents</div>
          <div class="card-desc">Manage distributed scanning agents deployed across your network.</div>
        </div>
        <div class="card-tag">AGENTS</div>
      </a>

      <a href="<%= reports_path rescue '#' %>" class="nav-card card-reports">
        <div class="card-arrow">→</div>
        <div class="card-icon-wrap">📊</div>
        <div>
          <div class="card-title">View Reports</div>
          <div class="card-desc">Review scan results, vulnerability findings, and remediation data.</div>
        </div>
        <div class="card-tag">REPORTS</div>
      </a>

      <a href="<%= settings_path rescue '#' %>" class="nav-card card-settings">
        <div class="card-arrow">→</div>
        <div class="card-icon-wrap">⚙️</div>
        <div>
          <div class="card-title">Settings</div>
          <div class="card-desc">Configure scanner options, API keys, notification preferences, and more.</div>
        </div>
        <div class="card-tag">CONFIG</div>
      </a>

    </div>

    <!-- Bottom Row -->
    <div class="bottom-row">
      <!-- Recent Activity -->
      <div class="panel">
        <div class="panel-header">
          <div class="panel-title">Recent Activity</div>
          <div class="panel-sub">LAST 24 HRS</div>
        </div>
        <div class="activity-item">
          <div class="activity-dot dot-blue"></div>
          <div>
            <div class="activity-text">Platform initialized and <strong>ready for scanning</strong></div>
            <div class="activity-time">just now</div>
          </div>
        </div>
        <div class="activity-item">
          <div class="activity-dot dot-green"></div>
          <div>
            <div class="activity-text">Asset management module <strong>loaded successfully</strong></div>
            <div class="activity-time">1 min ago</div>
          </div>
        </div>
        <div class="activity-item">
          <div class="activity-dot dot-green"></div>
          <div>
            <div class="activity-text">Database connection <strong>established</strong></div>
            <div class="activity-time">2 min ago</div>
          </div>
        </div>
        <div class="activity-item">
          <div class="activity-dot dot-orange"></div>
          <div>
            <div class="activity-text">No scans have been run yet — <strong>configure an asset to begin</strong></div>
            <div class="activity-time">—</div>
          </div>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="panel">
        <div class="panel-header">
          <div class="panel-title">Quick Actions</div>
          <div class="panel-sub">SHORTCUTS</div>
        </div>

        <div style="padding: 8px 0;">
          <a href="<%= new_asset_path %>" class="quick-btn">
            <div class="quick-btn-icon">➕</div>
            <span class="quick-btn-label">Add New Asset</span>
            <span class="quick-btn-arrow">→</span>
          </a>

          <a href="<%= scans_path rescue '#' %>" class="quick-btn">
            <div class="quick-btn-icon">▶️</div>
            <span class="quick-btn-label">Start New Scan</span>
            <span class="quick-btn-arrow">→</span>
          </a>

          <a href="<%= agents_path rescue '#' %>" class="quick-btn">
            <div class="quick-btn-icon">🤖</div>
            <span class="quick-btn-label">View Agents Status</span>
            <span class="quick-btn-arrow">→</span>
          </a>

          <a href="<%= reports_path rescue '#' %>" class="quick-btn">
            <div class="quick-btn-icon">📊</div>
            <span class="quick-btn-label">Latest Report</span>
            <span class="quick-btn-arrow">→</span>
          </a>

        </div>
      </div>
    </div>
  </div>

  <script>
    // Live clock
    function updateClock() {
      const now = new Date();
      document.getElementById('clock').textContent =
        now.toLocaleTimeString('en-US', { hour12: false });
    }
    updateClock();
    setInterval(updateClock, 1000);

    // Mouse-track glow on nav cards
    document.querySelectorAll('.nav-card').forEach(card => {
      card.addEventListener('mousemove', e => {
        const rect = card.getBoundingClientRect();
        const x = ((e.clientX - rect.left) / rect.width * 100).toFixed(1);
        const y = ((e.clientY - rect.top) / rect.height * 100).toFixed(1);
        card.style.setProperty('--mx', x + '%');
        card.style.setProperty('--my', y + '%');
      });
    });
  </script>
</body>
</html>
