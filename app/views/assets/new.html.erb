<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scan Configuration</title>
    <%= csrf_meta_tags %>
    <style>
        :root {
            --primary-blue: #3b82f6;
            --dark-blue: #1d4ed8;
            --bg-color: #f3f4f6;
            --border-color: #dee2e6;
            --text-color: #343a40;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            padding: 40px;
            margin: 0;
        }

        .card {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            width: 100%;
            max-width: 900px;
        }

        h1 {
            text-align: center;
            color: #1f2937;
            font-size: 24px;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f3f4f6;
        }

        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: var(--primary-blue);
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            column-gap: 40px;
            row-gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            margin-bottom: 10px;
        }

        label {
            font-weight: 600;
            font-size: 14px;
            color: #4b5563;
            margin-bottom: 8px;
        }

        label.required::after {
            content: " *";
            color: #ef4444;
        }

        input[type="text"],
        input[type="datetime-local"],
        select {
            padding: 12px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 14px;
            color: #333;
            transition: all 0.2s ease;
            background-color: #f9fafb;
        }

        input:focus, select:focus {
            outline: none;
            border-color: var(--primary-blue);
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }

        .boxed-group {
            background-color: #f9fafb;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            padding: 12px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            cursor: pointer;
            font-weight: 500;
        }

        input[type="radio"], input[type="checkbox"] {
            accent-color: var(--primary-blue);
            width: 16px;
            height: 16px;
            cursor: pointer;
        }

        .btn-group {
            margin-top: 40px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .btn-outline {
            background: white;
            border: 2px dashed #cbd5e1;
            color: #64748b;
            padding: 15px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 14px;
            width: 100%;
            text-align: center;
        }

        .btn-outline:hover {
            border-color: var(--primary-blue);
            color: var(--primary-blue);
            background-color: #eff6ff;
        }

        .btn-3d {
            position: relative;
            width: 100%;
            display: inline-block;
            cursor: pointer;
            outline: none;
            border: 0;
            vertical-align: middle;
            text-decoration: none;
            background: transparent;
            padding: 0;
            font-size: 16px;
            font-family: inherit;
        }

        .btn-3d-content {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 60px;
            padding: 0 20px;
            background: linear-gradient(to bottom, #3b82f6, #2563eb);
            border-radius: 12px;
            color: white;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: transform 0.1s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 2;
            border-bottom: 6px solid #1e40af; 
            box-shadow: 0 10px 20px -8px rgba(37, 99, 235, 0.5);
        }

        .btn-3d:hover .btn-3d-content {
            background: linear-gradient(to bottom, #4f8df9, #326bf5);
            transform: translateY(-2px);
        }

        .btn-3d:active .btn-3d-content {
            transform: translateY(4px);
            border-bottom-width: 2px;
            box-shadow: none;
        }

        .btn-3d.loading .btn-3d-content {
            background: #4b5563;
            border-bottom-color: #1f2937;
            pointer-events: none;
            transform: translateY(4px);
            border-bottom-width: 2px;
        }

        .spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
            margin-right: 10px;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        #file-upload-name {
            font-weight: bold;
            color: #2563eb;
        }
    </style>
</head>
<body>

    <div class="card">
        <%= link_to "← Back to Assets", assets_path, class: "back-link" %>
        
        <h1>Scan Configuration</h1>
        
        <%= form_with url: assets_path, method: :post, local: true, html: { id: 'scanForm' } do |f| %>
            <div class="form-grid">
                
                <div class="column">
                    <div class="form-group">
                        <label class="required">Scan Mode</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <%= radio_button_tag :scanMode, 'safe', false, required: true %>
                                Safe
                            </label>
                            <label class="radio-option">
                                <%= radio_button_tag :scanMode, 'exploits', false, required: true %>
                                Exploits
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="required">Target Scope</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <%= radio_button_tag :scope, 'individual', true, required: true %>
                                Individual Network
                            </label>
                            <label class="radio-option">
                                <%= radio_button_tag :scope, 'range', false, required: true %>
                                Range of Networks
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <%= label_tag :network, "Network / Network Range", class: "required" %>
                        <%= text_field_tag :network, nil, placeholder: "e.g. 192.168.1.1/24", required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :exclude, "Exclude Network", class: "required" %>
                        <%= text_field_tag :exclude, nil, placeholder: "e.g. 192.168.1.5", required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :port, "Port / Port Range", class: "required" %>
                        <%= text_field_tag :port, nil, placeholder: "e.g. 80, 443, 8000-8080", required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :os, "Operating System", class: "required" %>
                        <%= select_tag :os, options_for_select([
                            ['Select OS', ''],
                            ['Windows 10/11', 'win11'],
                            ['Windows Server 2019', 'server2019'],
                            ['Linux (Ubuntu/CentOS)', 'linux'],
                            ['macOS', 'macos']
                        ]), required: true %>
                    </div>
                </div>

                <div class="column">
                    <div class="form-group">
                        <%= label_tag :asset, "Asset Types", class: "required" %>
                        <%= select_tag :asset, options_for_select([
                            ['Server', 'server'],
                            ['Workstation', 'workstation'],
                            ['Network Device', 'network_device']
                        ]), required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :scanType, "Scan Type", class: "required" %>
                        <%= select_tag :scanType, options_for_select([
                            ['Full', 'full'],
                            ['Quick', 'quick'],
                            ['Custom', 'custom']
                        ]), required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :credential, "Credential", class: "required" %>
                        <%= text_field_tag :credential, nil, placeholder: "Enter credential ID or Path", required: true %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :schedule, "Schedule" %>
                        <%= datetime_local_field_tag :schedule, nil %>
                    </div>

                    <div class="form-group">
                        <%= label_tag :cve, "CVE ID" %>
                        <%= text_field_tag :cve, nil, placeholder: "e.g. CVE-2024-XXXX" %>
                    </div>

                    <div class="form-group">
                        <label class="required">Report Format</label>
                        <div class="boxed-group">
                            <label class="radio-option">
                                <%= check_box_tag 'format[]', 'json', true, required: true %>
                                JSON
                            </label>
                            <label class="radio-option">
                                <%= check_box_tag 'format[]', 'pdf', false %>
                                PDF
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="btn-group">
                <input type="file" id="jsonFile" accept=".json" style="display: none;">
                <button type="button" class="btn-outline" onclick="document.getElementById('jsonFile').click()">
                    📂 Import Asset JSON <span id="file-upload-name"></span>
                </button>

                <button type="submit" class="btn-3d" id="generateBtn">
                    <span class="btn-3d-content">
                        <span class="spinner"></span>
                        <span class="btn-text">Add Asset</span>
                    </span>
                </button>
            </div>
        <% end %>
    </div>

    <script>
        // File Upload Logic
        document.getElementById('jsonFile').addEventListener('change', function(e) {
            const fileName = e.target.files[0] ? e.target.files[0].name : "";
            const label = document.getElementById('file-upload-name');
            label.textContent = fileName ? `— ${fileName}` : "";
        });

        // Form Submit & Button Animation Logic
        document.getElementById('scanForm').addEventListener('submit', function(e) {
            // Don't prevent default - let Rails handle the submit
            // But show the loading animation
            
            const btn = document.getElementById('generateBtn');
            const spinner = btn.querySelector('.spinner');
            const text = btn.querySelector('.btn-text');

            // Add loading state
            btn.classList.add('loading');
            spinner.style.display = 'inline-block';
            text.textContent = 'Adding Asset...';
            
            // Rails will handle the actual form submission
        });
    </script>
</body>
</html>